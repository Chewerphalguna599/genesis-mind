# =============================================================================
# Genesis Mind — Dependencies (V8: Zero Pretrained Models)
# =============================================================================
# All models are from scratch. No CLIP, no Whisper, no SBERT, no Ollama.
# No CUDA, no GPU drivers, no cloud APIs, no internet required.
# =============================================================================

# --- Core Neural Framework ---
torch>=2.1.0                   # Backend for all from-scratch neural networks
torchaudio>=2.1.0              # Mel spectrogram computation (pure signal processing)
numpy>=1.24.0                  # Numerical backbone for all sensory data

# --- Perception (Senses) ---
opencv-python>=4.8.0           # Eyes: webcam capture and image processing
sounddevice>=0.4.6             # Ears: microphone audio capture
Pillow>=10.0.0                 # Image manipulation and format conversion

# --- Memory (Vector Database) ---
chromadb>=0.4.0                # Local vector database, no server needed

# --- Voice ---
# Neural vocoder only — no pretrained TTS (pyttsx3 removed)

# --- Utilities ---
scipy>=1.11.0                  # Signal processing for audio
flask==3.1.0
flask-cors==5.0.0
